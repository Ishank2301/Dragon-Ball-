# Copy this to .env and fill in any values you need.
# The app works without any env vars by default.

# Optional: override webcam device index (default is 0)
WEBCAM_INDEX=0

# Optional: override model path if you store it elsewhere
# MODEL_PATH=models/pose_landmarker_heavy.task
